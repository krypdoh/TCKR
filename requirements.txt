  # Core GUI Framework
  PyQt5>=5.15.0

  # HTTP Requests for API calls (Finnhub, stock data)
  requests>=2.28.0

  # Note: The following are built-in Python modules and don't need to be installed:
  # - sys, os, json, time, webbrowser, ctypes, concurrent.futures, argparse, shutil
