# Runtime dependencies
PyQt5>=5.15.0
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
pandas-market-calendars>=4.0.0
psutil>=5.9.0
websocket-client>=1.6.0
pip-system-certs>=4.0
pytz>=2023.3
colorama>=0.4.6

# Required for requests charset handling (PyInstaller runtime)
charset_normalizer>=3.0.0

# Used in setup_cython.py for building Cython extension
setuptools>=65.0.0

# Optional performance dependency (fallback if Cython extension not built)
numba>=0.57.0

# Build-time dependency for Cython extension (run: python setup_cython.py build_ext --inplace)
cython>=3.0.0  # Only needed for building Cython extension

# Optional packaging tool
pyinstaller>=6.0.0